# GuessGame - Provably Fair Blockchain Game

Консольная игра-угадайка на блокчейне TON с использованием смарт-контрактов на языке Tact и системой **provably fair** для гарантии честности игры.

**DISCLAIMER: Игра сделана в тест-нете TON, поэтому иногда может не срабатывать из-за ошибок RPC Testnet, просто запустите игру еще раз. Кошелек смарт-контракта тоже в testnet, так я не стал его прятать под переменные окружения.**

## 🎲 Как работает игра

### Правила игры
- Игрок делает ставку из локального депозита (минимум 0.01 TON)
- Игрок угадывает число от 1 до 5
- Сервер генерирует случайный результат используя **provably fair** алгоритм
- При выигрыше: выплата x2 от ставки
- При проигрыше: ставка сгорает
- Результат можно проверить независимо через блокчейн

### 🚀 Быстрый старт
```bash
# Установка зависимостей
npm install

# Запуск игры
npm start

# Тестирование контрактов
npx blueprint test

```

## 🔒 Система Provably Fair

Игра использует **цепочку хешей** для гарантии честности. Это означает, что сервер не может подделать результаты, а серверные исходные данные для вычисления результата сгенерированны заранее. Сервер не пытается обмануть игрока, подсовывая ему другой результат (заведомо проигрышный).

### Как это работает:

#### 1. **Инициализация сессии**
```
Сервер генерирует цепочку из 50 сидов:
seed[0] → seed[1] → seed[2] → ... → seed[49]
где seed[i+1] = sha256(seed[i])

Якорь = seed[0] (отправляется в смарт-контракт)
Публичный seed = seed[49] (показывается игроку)
```

**Зачем нужен якорь?**

Якорь (`seed[0]`) - это **начало цепочки**, которое фиксируется в блокчейне и **не может быть изменено**. Это гарантирует:

1. **Невозможность подмены цепочки** - сервер не может сгенерировать новую цепочку после того, как игрок сделал ставку
2. **Проверяемость всей сессии** - любой может проверить, что все сиды связаны с якорем:
   ```
   sha256(seed[0]) = seed[1]
   sha256(seed[1]) = seed[2]
   ...
   sha256(seed[48]) = seed[49]
   ```
3. **Публичная фиксация** - якорь записан в смарт-контракте на блокчейне, доступен всем. Сервер гарантирует, что последняя сыгранная сессия будет с этим якорем.
4. **Защита от мошенничества** - если сервер попытается использовать сид, не связанный с якорем, это будет обнаружено

#### 2. **Игровой процесс**
```
Игра идет в обратном порядке: seed[49] → seed[48] → seed[47] → ...

Для каждого раунда:
1. Игрок видит свой баланс в игре
2. Игрок видит Next Seed (commitment для следующего раунда) в формате hash
3. Игрок делает ставку (от 0.01 TON до всего баланса)
4. Ставка списывается с локального баланса
5. Игрок вводит число от 1 до 5
6. Сервер использует Current Seed для расчета результата  
7. При выигрыше: баланс пополняется на ставку × 2
8. При проигрыше: ставка остается списанной
9. Сервер раскрывает Current Seed и Room Seed
10. Игрок может проверить: hash(Next Seed) == Current Seed
```

**Зачем показывать следующий сид (Next Server Seed)?**

Следующий сид - это **commitment** (обязательство) сервера для следующего раунда. Это критически важно для честности:

1. **Доказательство предварительной генерации**
   - Сервер показывает `seed[48]` ДО того, как использует его в игре
   - Это доказывает, что сид был сгенерирован заранее, а не подобран под результат
   - Это доказывает, что сид для текущей игры был сгенерирован заранее из показанного игроку (который будет использован в следующей игре)

2. **Проверка цепочки**
   - После раунда игрок может проверить: `sha256(seed[48]) == seed[49]`
   - Это подтверждает, что сервер использовал правильный сид из цепочки
   - Если проверка не пройдет - сервер обманул!

3. **Защита от подмены**
   - Сервер не может изменить `seed[48]` после того, как показал его игроку
   - Если сервер попытается использовать другой сид - проверка цепочки провалится

**Пример:**
```
Раунд 1:
  Показываем: Next Seed = seed[48] (commitment)
  Используем: Current Seed = seed[49]
  Проверка: sha256(seed[48]) == seed[49] ✅

Раунд 2:
  Показываем: Next Seed = seed[47] (новый commitment)
  Используем: Current Seed = seed[48] (тот, что был commitment в раунде 1)
  Проверка: sha256(seed[47]) == seed[48] ✅
```

Без показа следующего сида сервер мог бы:
- ❌ Подбирать сид под нужный результат после ставки игрока
- ❌ Использовать сиды не из цепочки
- ❌ Генерировать новую цепочку после каждой игры

С показом следующего сида:
- ✅ Сервер обязан использовать заранее объявленный сид
- ✅ Игрок может проверить связь сидов в цепочке
- ✅ Невозможно подделать результат задним числом

**Почему показывать следующий сид безопасно?**

Может показаться, что если игрок знает следующий сид заранее (который будет использован в следующем раунде), он сможет предсказать результат. Но это **невозможно** благодаря формуле расчета:

```
result = hash(serverSeed + roomSeed + nonce) % 5 + 1
```

**Защита от предсказания:**

1. **Room Seed неизвестен игроку**
   - Генерируется сервером случайно для КАЖДОГО раунда (32 байта)
   - Игрок узнает Room Seed только ПОСЛЕ завершения игры
   - Даже зная Server Seed, без Room Seed невозможно вычислить результат

2. **Nonce меняется**
   - Каждый раунд имеет уникальный nonce (номер комнаты)
   - Даже если бы Room Seed был известен, nonce изменит результат

3. **Криптографическая стойкость SHA-256**
   - Невозможно подобрать входные данные для получения нужного хеша
   - Даже зная Server Seed, нельзя предсказать результат без Room Seed

**Пример:**
```
Раунд 2 (игрок знает Server Seed = seed[48] заранее):
  Server Seed: seed[48] = "a1b2c3d4..." (известен игроку)
  Room Seed:   ???????? (неизвестен!)
  Nonce:       2
  
  Результат = hash("a1b2c3d4..." + "????????" + "2") % 5 + 1
  
  Без Room Seed игрок НЕ МОЖЕТ вычислить результат! ❌
```

**Вывод:** Показывать следующий сид безопасно, потому что:
- ✅ Server Seed - это только ЧАСТЬ формулы
- ✅ Room Seed держится в секрете до конца игры
- ✅ Игрок не может предсказать результат, зная только Server Seed
- ✅ Но игрок МОЖЕТ проверить честность после игры, когда все данные раскрыты

#### 3. **Формула расчета результата**
```typescript
result = hash(serverSeed + roomSeed + nonce) % 5 + 1

где:
- serverSeed: из цепочки сидов (известен после игры)
- roomSeed: случайное число для каждой игры (32 байта)
- nonce: номер комнаты (увеличивается каждую игру)
```

### 🔍 Проверка честности

#### Что может проверить игрок:

1. **Цепочка сидов честная:**
   ```
   hash(Next Seed) == Current Seed ✅
   ```

2. **Результат вычислен правильно:**
   ```
   hash(Current Seed + Room Seed + Nonce) % 5 + 1 == Result ✅
   ```

#### Проверка через TonViewer:

После каждой игры вы получите:
1. **Адрес контракта** для проверки в TonViewer
2. **BOC данные** всех параметров (serverSeed, roomSeed, nonce, result)

**Методы смарт-контракта для проверки:**
- `verifyRoundSlice(serverSeed, roomSeed, nonce, claimedResult)` - проверяет правильность результата раунда
- `verifyChainLink(seedNext, seedCurrent)` - проверяет связь сидов в цепочке
- `get_anchor()` - получить якорь сессии из контракта

**Как проверить:**
1. Откройте ссылку на контракт в TonViewer (выдается после игры)
2. Перейдите в раздел "Methods" → "Get methods"
3. Выберите метод `verifyRoundSlice` или `verifyChainLink`
4. Вставьте BOC параметры из вывода игры
5. Нажмите "Run"
6. Результат `0x1` (true) = проверка пройдена ✅

### 📊 Пример вывода после игры

```
💰 ВАШ БАЛАНС В ИГРЕ:
   Адрес: EQB0BuWejK...gnbLqU
   Баланс: 0.0700 TON

🔐 COMMITMENT (следующий server seed):
   🔑 Следующий server seed: te6cckEBAQEAIgAAQJ00dj9E8/vF/4zc/g5L0GCSxwK2qXVaZY8D2OBjw9w+e2NA6w==

💡 Запишите этот seed! После раунда вы получите текущий seed для проверки.
   Проверка: текущий seed == этот commitment

🎮 Добро пожаловать в игру "Угадай число"!
   Сервер загадывает число от 1 до 5, вы делаете ставку и пытаетесь отгадать число.

💰 Введите ставку (от 0.01 до 0.0700 TON): 0.01

✅ Ставка 0.01 TON принята!
   Ваш баланс: 0.0600 TON

Введите число от 1 до 5: 3

[Server] 🎲 Начинаем раунд в комнате #2...
[Server] Использован seed: te6cckEBAQEAIgAAQDxGpTAvmgFkgDhto/Ke40S01ZhXczLXwfp6pW4z0X6QuNW5rQ==
[Server] 🔑 Server Seed (использован): te6cckEBAQEAIgAAQDxGpTAvmgFkgDhto/Ke40S01ZhXczLXwfp6pW4z0X6QuNW5rQ==
[Server] 📢 Следующий seed (commitment): te6cckEBAQEAIgAAQJ00dj9E8/vF/4zc/g5L0GCSxwK2qXVaZY8D2OBjw9w+e2NA6w==
[Server] 🏠 Room Seed: te6cckEBAQEAIgAAQBL7VdONcwBlZ+k28yVaxEMIsEZnZF35JrFJB1ksTCETfFWJeQ==

==================================================
🎲 РЕЗУЛЬТАТ ИГРЫ В КОМНАТЕ #2
==================================================
Выпало число: 3
Ваше число: 3

🎉 ПОЗДРАВЛЯЕМ! ВЫ УГАДАЛИ!

💰 Выигрыш добавлен к балансу!
   Выигрыш: 0.0200 TON
   Новый баланс: 0.0800 TON

==================================================
🔍 ДАННЫЕ ДЛЯ ПРОВЕРКИ ЧЕСТНОСТИ
==================================================
Nonce (комната): 2
Результат: 3

📋 ДЛЯ ПРОВЕРКИ РЕЗУЛЬТАТА В TONVIEWER:
Метод: verifyRoundSlice

Параметры:
Server Seed (Slice): te6cckEBAQEAIgAAQDxGpTAvmgFkgDhto/Ke40S01ZhXczLXwfp6pW4z0X6QuNW5rQ==
Room Seed (Slice): te6cckEBAQEAIgAAQBL7VdONcwBlZ+k28yVaxEMIsEZnZF35JrFJB1ksTCETfFWJeQ==
nonce (Number): 2
claimedResult (Number): 3

🔐 ДЛЯ ПРОВЕРКИ ЦЕПОЧКИ В TONVIEWER:
Метод: verifyChainLink

Параметры:
Следующий Server Seed (Slice): te6cckEBAQEAIgAAQJ00dj9E8/vF/4zc/g5L0GCSxwK2qXVaZY8D2OBjw9w+e2NA6w==
Текущий Server Seed (Slice): te6cckEBAQEAIgAAQDxGpTAvmgFkgDhto/Ke40S01ZhXczLXwfp6pW4z0X6QuNW5rQ==

💡 Проверяет: использованный сид был сгенерирован из следующего
   Результат должен быть 0x1 (true) если цепочка честная, если сервер обманул - 0x0 (false)
```

## 🛠️ Техническая архитектура

### Структура проекта
```
GuessGame/
├── contracts/           # Смарт-контракты на Tact
│   ├── guess.tact      # Основной контракт игры
│   ├── messages.tact   # Определения сообщений
│   └── utils.tact      # Утилиты для хеширования
├── scripts/
│   ├── game/           # Игровая логика
│   │   ├── server.ts   # Генерация сидов и управление сессиями
│   │   ├── client.ts   # Клиентская логика (ввод, отображение)
│   │   ├── gameflow.ts # Основной игровой цикл
│   │   ├── wallet.ts   # Работа с кошельком и депозитами
│   │   ├── history.ts  # Сохранение истории игр
│   │   └── seqnoUtils.ts # Управление транзакциями
│   └── utils/
│       ├── bocUtils.ts # Утилиты для работы с BOC
│       ├── seedsStorage.ts # Работа с файлом seeds
│       └── depositMonitor.ts # Мониторинг депозитов
├── tests/              # Тесты контрактов
└── data/               # Локальные данные
    ├── seeds.json      # Цепочка сидов
    ├── history.json    # История раундов
    ├── players.json    # Балансы игроков
    └── lastLT.json     # Последняя транзакция
```

### Ключевые функции контракта

| Функция | Описание |
|---------|----------|
| `calculateResult` | Вычисляет результат игры |
| `verifyRoundSlice` | Проверяет честность раунда |
| `verifyChainLink` | Проверяет связь в цепочке сидов |

### Файлы данных

- **`data/seeds.json`** - Цепочка сидов для текущей сессии (удаляются по мере использования)
- **`data/history.json`** - История всех раундов с BOC данными и якорем сессии
- **`data/players.json`** - Локальные балансы игроков (депозиты, выигрыши)
- **`data/lastLT.json`** - Последняя обработанная транзакция (для мониторинга депозитов)

### Система депозитов и ставок

#### 💰 Локальный баланс
- Игрок пополняет баланс через `Deposit` сообщение в контракт
- Баланс хранится локально в `players.json`
- Все операции со ставками мгновенные (без блокчейна)
- Вывод средств через `Withdraw` сообщение в контракт

#### 🎲 Механика ставок
- Минимальная ставка: **0.01 TON**
- Максимальная ставка: **весь баланс игрока**
- Выигрыш: **ставка × 2** (чистая прибыль = ставка)
- Проигрыш: **ставка сгорает**

#### 🔄 Процесс депозита
```typescript
1. Игрок отправляет Deposit в контракт
2. Контракт эмитит DepositEvent
3. Локальный монитор обновляет баланс в players.json
4. Игрок может делать ставки из локального баланса
```

#### 💸 Процесс вывода
```typescript
1. Игрок запускает игру с withddrawAll = true
2. Отправляется Withdraw сообщение в контракт
3. Контракт переводит TON на адрес игрока
4. Локальный баланс обнуляется
```

## 🛡️ Безопасность и честность

### Что записывается в блокчейн:
- ✅ **Якорь сессии** (`seed[0]`) - фиксируется в смарт-контракте через `NewSession`
- ✅ **Код смарт-контракта** - формулы расчета результата публичны и неизменны
- ❌ **Результаты игр** - НЕ записываются в блокчейн (только локально в `history.json`)
- ❌ **Использованные сиды** - НЕ записываются в блокчейн (только локально)

**Почему результаты не в блокчейне?**
- Экономия на комиссиях (каждая запись стоит TON)
- Игрок может проверить честность через смарт-контракт методами `verifyRoundSlice` и `verifyChainLink`
- История сохраняется локально с BOC данными для проверки

### ⚠️ Потенциальные уязвимости:

1. **Локальное хранение истории**
   - ❌ История в `data/history.json` может быть изменена сервером
   - ✅ **Защита**: Игрок должен сохранить BOC данные сразу после игры
   - ✅ **Защита**: Проверка через смарт-контракт независима от локальной истории

2. **Сервер может не раскрыть сиды**
   - ❌ Сервер может отказаться показать использованные сиды
   - ✅ **Защита**: Якорь в блокчейне позволяет вычислить всю цепочку
   - ⚠️ **Ограничение**: Требует вычислительных ресурсов (50 хешей)

3. **Локальное хранение балансов**
   - ❌ Балансы игроков хранятся локально в `players.json`
   - ❌ Сервер может изменить баланс игрока
   - ✅ **Защита**: Депозиты и выводы проходят через смарт-контракт
   - ✅ **Защита**: Игрок может вывести средства в любой момент
   - ⚠️ **Ограничение**: Ставки и выигрыши не записываются в блокчейн (экономия gas)

### 🎯 Вывод по безопасности:

**Сложно взломать:**
- ✅ Подделать результаты прошлых игр - **невозможно** (якорь в блокчейне)
- ✅ Изменить формулу расчета - **невозможно** (контракт неизменяем)
- ✅ Предсказать результат - **невозможно** (неизвестен Room Seed и Nonce)

**Текущая реализация:**
- ✅ Депозиты и выводы через смарт-контракт
- ✅ Локальные балансы для быстрых ставок
- ✅ Provably fair механизм с проверкой
- ⚠️ Требует доверия к серверу для честного учета ставок

**Рекомендации для полной децентрализации:**
1. Записывать каждую ставку в блокчейн (высокие комиссии)
2. Автоматическая выплата через контракт при проверке результата
3. Записывать commitment'ы в блокчейн для каждого раунда

## 🎯 Что делает систему Provably Fair

### ✅ Сервер НЕ МОЖЕТ:
- **Подделать результат после ставки** - commitment зафиксирован до игры
- **Изменить цепочку сидов** - якорь записан в блокчейне
- **Использовать сид не из цепочки** - проверка через `verifyChainLink`
- **Подменить формулу расчета** - смарт-контракт неизменяем

### ✅ Игрок МОЖЕТ:
- **Проверить честность любого раунда** - через `verifyRoundSlice` в TonViewer
- **Проверить связь сидов в цепочке** - через `verifyChainLink` в TonViewer
- **Убедиться что результат вычислен правильно** - пересчитать самостоятельно
- **Сделать это независимо** - проверка через блокчейн, не зависит от сервера

### 📋 Соответствие стандарту Provably Fair:

| Требование | Статус | Реализация |
|------------|--------|------------|
| Commitment до игры | ✅ | Next Seed показывается перед ставкой |
| Reveal после игры | ✅ | Current Seed раскрывается после раунда |
| Проверяемость | ✅ | `verifyRoundSlice` + `verifyChainLink` в контракте |
| Детерминизм | ✅ | Публичная формула расчета в контракте |
| Невозможность подмены | ✅ | Якорь зафиксирован в блокчейне |
| Невозможность предсказания | ✅ | Room Seed неизвестен до конца игры |

### 🔐 Четыре столпа Provably Fair:

1. **Цепочка хешей** - сиды генерируются ДО игры, якорь фиксируется в блокчейне
2. **Commitment-Reveal** - commitment показывается ПЕРЕД ставкой, reveal ПОСЛЕ игры
3. **Детерминированный расчет** - публичная формула в смарт-контракте
4. **Защита от предсказания** - Room Seed неизвестен до конца игры

## 🔗 Полезные ссылки

- **Контракт в Testnet:** https://testnet.tonviewer.com/kQAij42o-qafpdRFkSRhQ16G5FHKfxy_Ynl6jZfTA7ZATAE_
- **Проверка методов контракта:** https://testnet.tonviewer.com/kQAij42o-qafpdRFkSRhQ16G5FHKfxy_Ynl6jZfTA7ZATAE_?section=method
- **TON Documentation:** https://docs.ton.org/
- **Tact Language:** https://docs.tact-lang.org/
- **Blueprint Framework:** https://github.com/ton-org/blueprint

---

**Игра полностью прозрачна и честна. Каждый результат можно независимо проверить!** 🎲✨
